# Example Supabase environment variables for MedflowAI services

# Supabase hosted project (leave blank for local docker-compose)
SUPABASE_URL=https://xyz.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
JWT_SECRET=

# Local compose credentials (keep in sync with docker-compose.dev.yml)
POSTGRES_PASSWORD=postgres
